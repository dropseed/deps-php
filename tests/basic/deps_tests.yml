tests:
- name: Basic test
  repo: repo_base
  diff: repo_final
  data: dependencies.json
  diff_args: ["-x", "vendor"]
  user_config:
    path: /
    # settings:
      # composer_options: "--ignore-platform-reqs"
